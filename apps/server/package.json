{
  "name": "@central/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch --env-file ../../.env src/index.ts",
    "start": "bun --env-file ../../.env src/index.ts",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@central/shared": "workspace:*",
    "@central/node": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
