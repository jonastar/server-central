{
  "name": "@central/server",
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch --env-file ../../.env src/index.ts",
    "start": "bun --env-file ../../.env src/index.ts",
    "test": "bun test",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit && bunx tsc -p test/tsconfig.json --noEmit"
  },
  "dependencies": {
    "@central/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
